{
  "name": "agent-bridge",
  "version": "1.0.0",
  "description": "MCP daemon for async messaging between coding agents (Claude Code, Codex, OpenCode)",
  "license": "MIT",
  "keywords": [
    "mcp",
    "agent",
    "claude-code",
    "codex",
    "opencode",
    "daemon",
    "bun"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ruipedro-pinheiro/agent-bridge.git"
  },
  "homepage": "https://github.com/ruipedro-pinheiro/agent-bridge#readme",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --skipLibCheck --types bun --allowImportingTsExtensions --moduleResolution bundler --module preserve --target es2022 src/*.ts scripts/*.ts tests/*.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
